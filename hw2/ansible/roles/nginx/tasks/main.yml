---
- name: Install Nginx
  apk:
    name: nginx
    state: present
    update_cache: yes

- name: Copy Nginx config
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/nginx.conf

- name: Run Nginx
  service:
    name: nginx
    state: started
    enabled: yes
